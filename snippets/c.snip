# OpenMPI
snippet MPI_Init(
abbr    MPI_Init(&argc, &argv); MPI_Finalize();
	MPI_Init(${1:&argc}, ${2:&argv});
	${0}
	MPI_Finalize();

snippet MPI_Comm_rank(
	MPI_Comm_rank(${1:MPI_COMM_WORLD}, ${2:#:&rank});

snippet MPI_Comm_size(
	MPI_Comm_size(${1:MPI_COMM_WORLD}, ${2:#:&size});
